server:
  port: 8762
spring:
  application:
    name: nativeconfigserver
  #配置⽂件的获取方式(本地)
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          #本地配置文件的存放路径
          search-locations: classpath:/shared