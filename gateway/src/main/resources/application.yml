server:
  port: 8083
spring:
  application:
    #服务名
    name: geteway
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
#自定义微服务访问路径
zuul:
  routes:
    provider: /p/**
